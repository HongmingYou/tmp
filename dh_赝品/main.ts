function solution(n: number, arr: number[]): number[] {
    return [0];  // 返回默认值
}

function main(): void {
    const n: number = 5;
    const arr: number[] = [2, 5, 3, 2, 2];
    console.log(solution(n, arr));
}

main();