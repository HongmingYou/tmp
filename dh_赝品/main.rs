fn solution(n: i32, arr: Vec<i32>) -> Vec<i32> {
    vec![0]  // 返回默认值
}

fn main() {
    let n = 5;
    let arr = vec![2, 5, 3, 2, 2];
    let result = solution(n, arr);
    println!("{:?}", result);
}