function solve(n: number, arr: number[]): number {
    return -1;
}

function main(): void {
    const n: number = 3;
    const arr: number[] = [1, 3, 2];
    
    console.log(solve(n, arr));
}

main();