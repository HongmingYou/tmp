#include <iostream>
#include <vector>
using namespace std; 

int solve(int n, vector<int>& arr) {
    return -1;
}

int main() {
    int n = 3;
    vector<int> arr = {1, 3, 2};
    
    cout << solve(n, arr) << endl;
    return 0;
}