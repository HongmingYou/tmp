#include <iostream>
#include <vector>
using namespace std; 

int solution(int n, int alpha, vector<int>& weights) {
    // 具体实现逻辑
    return 0;
}

int main() {
    int n = 5;
    int alpha = 53;
    vector<int> weights = {50, 52, 55, 53, 55};
    cout << solution(n, alpha, weights) << endl;  // 预期输出: 1
    return 0;
}