function solution(n, alpha, weights) {
    // 具体实现逻辑
    return 0;
}

function main() {
    const n = 5;
    const alpha = 53;
    const weights = [50, 52, 55, 53, 55];
    console.log(solution(n, alpha, weights));  // 预期输出: 1
}

main();