fn solution(n: i32, alpha: i32, weights: Vec<i32>) -> i32 {
    // 具体实现逻辑
    0
}

fn main() {
    let n = 5;
    let alpha = 53;
    let weights = vec![50, 52, 55, 53, 55];
    println!("{}", solution(n, alpha, weights));  // 预期输出: 1
}