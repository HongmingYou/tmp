function solution(n: number, alpha: number, weights: number[]): number {
    // 具体实现逻辑
    return 0;
}

function main() {
    const n: number = 5;
    const alpha: number = 53;
    const weights: number[] = [50, 52, 55, 53, 55];
    console.log(solution(n, alpha, weights));  // 预期输出: 1
}

main();